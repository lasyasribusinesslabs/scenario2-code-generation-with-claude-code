{
  "_comment": "PERSONAL MCP CONFIG - Copy this into ~/.claude.json on your local machine. DO NOT commit real tokens. Example only.",
  "_scope": "personal (user-scoped, stored in ~/.claude.json, NOT version-controlled)",
  "mcpServers": {
    "stripe": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-stripe"],
      "env": {
        "STRIPE_SECRET_KEY": "${STRIPE_SECRET_KEY}"
      },
      "description": "Personal Stripe MCP. Key from shell environment, never hardcoded."
    }
  }
}
